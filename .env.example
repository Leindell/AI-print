# Example environment configuration for AI Print Studio

# Telegram bot configuration
BOT_TOKEN=your-telegram-bot-token
MANAGER_CHAT_ID=123456789

# Shared secret used by the backend to authenticate requests to the bot.  Keep
# this value secret and identical in both the backend and the bot.
BOT_SECRET=supersecret

# URL where the backend should forward incoming orders.  By default the bot
# runs on localhost:8000 when using uvicorn with the provided commands.
TELEGRAM_SERVICE_URL=http://localhost:8000/api/bot/orders

# Optional: URL on your backend to receive status updates from the bot when
# a manager accepts, clarifies or rejects an order.  The `{id}` placeholder
# will be replaced with the order ID.
SERVER_STATUS_ENDPOINT=http://localhost:5000/api/orders/{id}/status